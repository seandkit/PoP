<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.testTagSearching"></activity>
        <activity
            android:name=".activity.ExportActivity"
            android:theme="@style/AppTheme.PopMe" />
        <activity android:name=".activity.main2Test" />
        <activity
            android:name=".activity.Receipt_main_activity"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity android:name="activity.ReceiptActivity">

        </activity>
        <activity android:name=".activity.RecentTransactionsActivity">

        </activity>
        <activity android:name=".activity.receiptTransaction" />
        <activity android:name=".activity.LoginActivity">

        </activity>
        <activity android:name=".activity.RegisterActivity">

        </activity>
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>